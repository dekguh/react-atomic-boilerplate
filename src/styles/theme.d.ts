export declare module '@mui/material/styles' {
  interface Theme {
    text: {
      heading: string;
      body: string;
    }
  }

  interface ThemeOptions {
    text: {
      heading: string;
      body: string;
    }
  }
}