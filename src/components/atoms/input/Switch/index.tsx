// MUIS
import SwitchMUI, { SwitchProps } from '@mui/material/Switch'
import { styled } from '@mui/material/styles'

const Switch = styled(SwitchMUI)<SwitchProps>({
  fontSize: 14,
})

export default Switch