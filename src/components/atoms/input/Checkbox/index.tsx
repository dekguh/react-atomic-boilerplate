// MUIS
import CheckboxMUI, { CheckboxProps } from '@mui/material/Checkbox'
import { styled } from '@mui/material/styles'

const Checkbox = styled(CheckboxMUI)<CheckboxProps>({
  fontSize: 14,
})

export default Checkbox