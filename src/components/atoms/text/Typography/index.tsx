// MUIS
import TypographyMUI, { TypographyProps } from '@mui/material/Typography'
import { styled } from '@mui/material/styles'

const Typography = styled(TypographyMUI)<TypographyProps>({
  fontSize: 14,
})

export default Typography