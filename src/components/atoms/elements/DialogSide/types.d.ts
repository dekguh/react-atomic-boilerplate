// MUI TYPES
import { BackdropProps } from '@mui/material/Backdrop'
import { SlideProps } from '@mui/material/Slide'

export interface IDialogSideProps {
  backdropProps: BackdropProps;
  slideProps: SlideProps;
}