// MUIS
import TooltipMUI, { TooltipProps } from '@mui/material/Tooltip'
import { styled } from '@mui/material/styles'

export const Tooltip = styled(TooltipMUI)<TooltipProps>({})

export default Tooltip